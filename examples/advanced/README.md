# Advanced Examples

- `multi_deck_batch.sh` – run the pipeline across multiple decks, syncing artifacts into `pipeline_results/`.
- `custom_filters.yml` – sample deny lists + stop words for noisy decks. Pass via `--config`.
