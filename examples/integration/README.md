# Integration Examples

- `whisper_example.py` – loads prompt terms and prints a comma-separated hint suitable for Whisper's `initial_prompt`.
- `google_stt_example.py` – prints the PhraseSet JSON for use with Google Speech-to-Text v2 Speech Adaptation APIs.
