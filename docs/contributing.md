# Contributing

1. Create a virtualenv and install dev deps (`pip install -r requirements.txt`).
2. Run formatting/linting as needed (`ruff`, `black`, etc. – configure to taste).
3. Add unit tests under `tests/` and synthetic fixtures only.
4. Run `pytest` before opening a PR.
5. Document new features in `docs/` and add release notes to `CHANGELOG.md`.
6. Submit PR with checklist + link to relevant issue.
